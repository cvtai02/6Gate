# API Clients

Shared TypeScript DTOs and typed client functions consumed by `ui/` and implemented by `app/`.

Includes DTOs and methods for providers, accounts, destinations, groups, group upload queues, jobs, and runtime settings.
