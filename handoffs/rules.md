# Handoff Rules

- Backend to UI handoff is required for API, DTO, endpoint, or auth changes.
- UI to backend handoff is required for new backend requirements.
- Move completed handoffs to `handoffs/archive/`.

