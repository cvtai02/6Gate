# Uploads Rules

- Multipart uploads are temporary local files and do not create publish jobs by themselves.
- Upload routes return local paths only; group/job modules decide how paths are published.
