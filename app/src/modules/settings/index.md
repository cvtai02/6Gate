# Settings Module

JSON-backed runtime settings.

Runtime settings are persisted to `env.settingsPath`, which defaults to the local 6Gate runtime config directory. The settings API exposes the file as key/value rows for the UI.
