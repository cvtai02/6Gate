# API MCP Server Rules

- Use this folder for smoke tests and operational tools only.
- Do not duplicate business logic from `app/`.
- Update `api-clients/` before adding tools that depend on new API contracts.

